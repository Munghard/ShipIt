using UnityEngine;
public static class ProgressBarColors
{
    public static readonly Color Green  = new Color(0.45f, 0.80f, 0.60f); // Soft, modern green
    public static readonly Color Red    = new Color(0.85f, 0.40f, 0.40f); // Muted red, not overly aggressive
    public static readonly Color Cyan   = new Color(0.50f, 0.85f, 0.90f); // Light cyan matching secondary
    public static readonly Color Yellow = new Color(0.95f, 0.82f, 0.45f); // Warm soft yellow, not too bright
}
